#import "/lib.typ" : *
== Dot Product

#definition[
  For a vector $bold(a)$ in n dimensions with components $chevron a_1, a_2, dots.c, a_i, dots.c, a_n chevron.r,$ and vector $bold(b)$ also in n dimensions with components $chevron b_1, b_2, dots.c, b_i, dots.c, b_n chevron.r,$ the dot product between $bold(a)$ and $bold(b),$ $bold(a) dot bold(b) := $
]

#theorem[
  
]
#proof[
  
]


// #cetz.canvas({
//   import cetz.draw: *
//   import cetz-plot: *

//   plot.plot(size: (8, 6), axis-style: "school-book", {
//     plot.add(
//       domain: (-0.3, 0.3),
//       x => calc.pow(x, 2) * calc.sin(1 / x),
//       samples: 200,
//     )
//   })
// })
