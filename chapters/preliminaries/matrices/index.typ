#import "/lib.typ": *

== Matrices

One of the motivations to have matrices is when transforming coordinate systems. 
$
mat(
  2, 3, 3;
  2, 4, 5;
  1, 1, 2
)
mat(
  x_1;
  x_2;
  x_3
)
=
mat(
  u_1;
  u_2;
  u_3
)
$